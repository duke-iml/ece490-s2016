This data/ folder is not updated by Git, so place any big files
here, like videos, datasets, etc that should not be shared between teams.

A rule of thumb is that if it's greater than 100mb, don't upload it to Git.

